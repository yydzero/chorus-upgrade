{{#each models}}
    <li data-id="{{id}}">
        <a class="image">
            <img src="{{imageUrl}}"/>
        </a>
        <div class="main">
            <div class="text">
                <a href="{{showUrl}}" class="name">
                    <span title="{{name}}">{{name}}</span>
                </a>
            </div>
        </div>
    </li>
{{/each}}
